'use client'

import React from 'react'
import CollaborationBoard from '@/components/CollaborationBoard'

/**
 * Collaboration View
 * Shows comments and activity feed for the learnership group
 */
export default function CollaborationView() {
  return (
    <div>
      <CollaborationBoard />
    </div>
  )
}
